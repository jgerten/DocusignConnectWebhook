version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: docusign-postgres
    environment:
      POSTGRES_DB: docusign_webhook
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres123
    ports:
      - "6432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: docusign-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # Uncomment to run the API in Docker
  # api:
  #   build:
  #     context: .
  #     dockerfile: src/DocuSignWebhook.API/Dockerfile
  #   container_name: docusign-api
  #   environment:
  #     ASPNETCORE_ENVIRONMENT: Development
  #     ConnectionStrings__DefaultConnection: "Host=postgres;Port=6432;Database=docusign_webhook;Username=postgres;Password=postgres123"
  #     DocuSign__AccountId: "your_account_id"
  #     DocuSign__AccessToken: "your_access_token"
  #     DocuSign__BasePath: "https://demo.docusign.net/restapi"
  #     DocuSign__HmacSecret: "your_hmac_secret"
  #     MinIO__Endpoint: "minio:9000"
  #     MinIO__AccessKey: "minioadmin"
  #     MinIO__SecretKey: "minioadmin"
  #     MinIO__UseSSL: "false"
  #   ports:
  #     - "5081:8080"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     minio:
  #       condition: service_healthy

volumes:
  postgres_data:
  minio_data:
